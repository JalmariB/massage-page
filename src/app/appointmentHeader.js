var React = require('React');


var AppointmentHeader = React.createClass({
  render: function(){
    return (
      <div className="appoinment-header-container">
        <h1>AJANVARAUS</h1>
      </div>

    );
  }
});

module.exports = AppointmentHeader;
